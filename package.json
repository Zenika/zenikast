{
  "name": "maif-ondemand",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "yarn --cwd ./proxy && yarn --cwd ./backend && yarn --cwd ./frontend",
    "start": "yarn --cwd ./backend start",
    "start-dev": "yarn --cwd ./proxy start & yarn --cwd ./frontend start"
  }
}
